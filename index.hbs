{{!< default}}
{{!-- Post feed page --}}

<div class="container">
    {{#if @site.description}}
        <div class="page-header">
            <div class="page-title">{{@site.description}}</div>
        </div>
    {{/if}}

    {{> "loop"}}
</div>
